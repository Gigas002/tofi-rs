[package]
name = "libtofi-rs"
version = "0.1.0"
edition.workspace = true

[dependencies]
thiserror = "2"
unicode-normalization = "0.1"
nix = { version = "0.29", features = ["fs"] }
walkdir = "2"
tracing = "0.1"
# Wayland stack — all three must resolve to the same wayland-client major (§3.1).
wayland-client = { version = "0.31", optional = true }
wayland-protocols = { version = "0.32", features = ["client", "staging"], optional = true }
wayland-protocols-wlr = { version = "0.3", features = ["client"], optional = true }

[dev-dependencies]
tempfile = "3"

# §4.1 / docs/RUST_MIGRATION_PLAN.md — engine + renderer feature split.
[features]
default = [
    "wayland",
    "renderer",
    "drun",
    "clipboard",
    "single-instance-lock",
]
wayland = ["dep:wayland-client", "dep:wayland-protocols", "dep:wayland-protocols-wlr"]
renderer = []
drun = []
# Paste implementation lives under `wayland::clipboard`; enabling implies `wayland`.
clipboard = ["wayland"]
single-instance-lock = []
